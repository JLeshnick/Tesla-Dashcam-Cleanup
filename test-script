import os

# --- PASTE THE PATH TO ONE SPECIFIC FAILED FOLDER HERE ---
test_folder = r"D:\TeslaCam\SentryClips\2025-12-12_10-30-12" 
# ---------------------------------------------------------

print(f"--- DIAGNOSTIC FOR: {test_folder} ---")

if not os.path.exists(test_folder):
    print("ERROR: Python cannot find this folder at all. Check the path.")
else:
    print("Folder exists. Listing contents:")
    try:
        files = os.listdir(test_folder)
        for f in files:
            print(f" - Found file: '{f}'")
            if f.lower() == "thumb.png":
                print(f"   -> MATCH! This file is valid but was named: {f}")
                if f != "thumb.png":
                    print("   -> CAUSE FOUND: Case mismatch (thumb.png vs Thumb.png)")
            else:
                print("   -> No match.")
    except Exception as e:
        print(f"Error accessing files: {e}")